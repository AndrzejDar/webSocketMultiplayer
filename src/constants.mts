export const PLAYER_RADIUS = 10;
export const PLAYER_COLOR = "red";
export const TICK_LENGTH = 60;

export const CANVAS_WIDTH = 800;
export const CANVAS_HEIGHT = 800;
export const FPS_LIMIT = 100;

// export default { PLAYER_RADIUS };
